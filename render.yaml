services:
  - type: web
    name: lms-socket-server
    env: node
    plan: free
    region: oregon
    branch: main
    buildCommand: npm install
    startCommand: npm run ws-server
    envVars:
      - key: PORT
        value: "3000"
    healthCheckPath: /
    autoDeploy: true

